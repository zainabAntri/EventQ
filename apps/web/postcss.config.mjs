/**
 * Tailwind CSS v4 is configured entirely from CSS (`@import 'tailwindcss'` and
 * the `@theme` block in globals.css). There is no tailwind.config.js.
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
