/**
 * The words EventQ uses to describe itself: page titles, share cards and
 * structured data all read from here, so they cannot drift apart.
 */
export const SITE_NAME = 'EventQ';

export const SITE_TITLE = 'EventQ - Audience Q&A for live events';

export const SITE_DESCRIPTION =
  'Collect, moderate and rank audience questions in real time. Attendees scan a QR code and ask - no app, no account.';

/** The brand blue (--color-brand-600 in globals.css) as hex, for image
 *  generation, which cannot read CSS variables or oklch(). */
export const BRAND_HEX = '#1d5fd0';
